rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Temporarily allow all reads and writes for authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
